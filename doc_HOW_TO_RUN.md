# 실행 절차

모든 명령은 `/home/karma/camera` 에서 `./venv/bin/python` 으로 실행한다.

준비물: **자(또는 줄자)**, **Galaxy S24**, 곧은 모서리(자·책등) 하나.
자가 없으면 미터 단위 좌표는 원리적으로 나오지 않는다.

---

## 1단계 — 캘리브레이션 타겟 만들기

```bash
./venv/bin/python z_calibrate.py --make-target
```

`target_s24.png` (2340x1080) 가 생긴다. 이 파일을 S24로 옮긴다.

**폰에서:**
1. 사진을 **전체화면**으로 연다.
2. **폰을 가로로 눕힌다.** (세로로 쓰면 보드가 2.4배 작아져서 캘리 품질이 떨어진다)
3. 화면 밝기를 **중간 이상**으로 올린다. 너무 어두우면 PWM 조광 때문에
   카메라에 줄무늬가 생긴다.
4. 화면 위쪽에 **세로 막대 2개**가 보인다. 그 **왼쪽 모서리 사이 거리를 자로 잰다.**
   - 예상 범위는 **121.5 ~ 122.2 mm**. 이 범위를 크게 벗어나면 갤러리 앱이
     이미지를 1:1로 안 띄운 것인데, 그래도 **실측값을 그대로 쓰면 된다.**
     그게 이 기준막대를 넣은 이유다.
   - 이 한 번의 측정이 ppi 스펙 불확실성(418/416/415.7)과 앱 스케일링을
     동시에 제거한다.

---

## 2단계 — 캘리 이미지 촬영

```bash
./venv/bin/python z_calibrate.py --capture
```

라이브 창이 뜬다. HUD 읽는 법:

| 표시 | 의미 | 목표 |
|---|---|---|
| `board: FOUND` | 체커보드 검출됨 | 이 상태에서만 저장된다 |
| `sharp:` | 선명도 (라플라시안 분산) | 클수록 좋다. 급락하는 거리는 버린다 |
| `size :` | 보드 폭 / 이미지 폭 | **30% 이상** |

**먼저 거리를 찾는다.** 폰을 앞뒤로 움직이며 `sharp` 와 `size` 를 본다.
이 웹캠이 고정초점이라 너무 붙이면 흐려진다. **선명하면서 size 30% 이상**인
거리를 찾는다 (계산상 약 31cm 부근).

**그 다음 15~20장을 찍는다.** SPACE 로 촬영.
- **매번 각도를 바꾼다.** 정면만 찍으면 초점거리와 거리가 분리되지 않아
  (구조적 모호성) K가 엉뚱하게 나온다. 좌우·상하로 20~40도씩 기울인다.
- **화면 가장자리와 네 모서리에도** 보드를 가져간다. 왜곡계수는 가장자리
  데이터로만 추정된다. HUD에 지난 촬영 위치가 옅은 선으로 남으니 빈 곳을 채운다.
- 방 조명을 줄이고 각도를 틀어 화면 반사를 피한다.

Q 로 종료.

## 3단계 — 캘리브레이션 실행

```bash
./venv/bin/python z_calibrate.py --run --ruler-mm 122.0    # 1단계에서 잰 값
```

`calib.json` 이 생긴다. 합격선:

| 게이트 | 기준 |
|---|---|
| 재투영 RMS | < 1.0 px |
| 뷰 수 | >= 15 |
| 커버리지 | >= 12/16 칸 |

미달이면 나쁜 뷰를 `calib_shots/` 에서 지우고 다시 `--run`, 또는 재촬영.

---

## 4단계 — 본 촬영 (권장: 체커보드 방식)

측정할 물체를 놓고, **폰에 `target_s24.png` 를 띄워 물체 옆에 세워둔다.**

```bash
./venv/bin/python z_capture.py --board
```

1. SPACE — 첫 번째 촬영 (HUD에 `board: FOUND` 떠 있을 때만 저장된다)
2. **카메라를 옆으로 옮긴다. 아무렇게나 옮겨도 된다.**
   들어 올려도 되고, 회전이 섞여도 되고, 거리를 재지 않아도 된다.
3. SPACE — 두 번째 촬영. Q 로 종료.

**자도, 곧은 모서리도, 힌지 고정도, 회전 주의도 필요 없다.**
두 사진의 체커보드에서 카메라 자세를 `solvePnP` 로 직접 재기 때문이다.
스케일은 이미 확정된 사각형 실치수(8.2012mm)에서 나온다.

**지켜야 할 것은 세 개뿐:**
- 폰(보드)이 **두 사진 모두에 가려지지 않고 온전히** 보일 것
- 두 촬영 사이에 **폰과 물체가 움직이지 않을 것** — 카메라만 움직인다
- 옆으로 옮기는 거리는 **대상까지 거리의 1/10 이상** (깊이 정확도 때문)

### "폰을 움직이는 것"과 "카메라를 움직이는 것"은 완전히 다르다

시차는 **카메라가 움직여야** 생긴다. 폰만 움직이면 물체는 두 사진에서 똑같이
찍히는데(시차 0), 보드는 그 움직임을 "카메라가 움직였다"고 해석해 가짜 baseline 을
만들어낸다. 합성 검증(참값 200x150mm / 0.800m):

| 무엇이 움직였나 | 복원 거리 | 치수 | 오차 |
|---|---|---|---|
| **카메라만** (정상) | 0.794 m | 195.0 x 145.7 | 2.5% |
| 폰만 (카메라 고정) | 48.87 m | 12573 x 5966 | 6186% |
| 둘 다 | 0.952 m | 240.5 x 174.8 | **20.2%** |

**둘 다 움직인 경우가 제일 위험하다** — 그럴듯한 숫자가 20% 틀리게 나온다.

그래서 촬영 직후 **보드 일관성 검사**가 자동으로 돈다. 보드를 뺀 배경 특징점으로
카메라 이동을 독립적으로 구해 보드가 말하는 것과 대조한다.

| 검사 | 기준 | 잡아내는 것 |
|---|---|---|
| 배경 이동 | >= 2 px | 카메라가 아예 안 움직인 경우 (폰만 이동) |
| 회전 일치 | < 2 deg | 폰이 회전한 경우 |
| 이동방향 일치 | < 3 deg | 폰이 카메라와 다른 방향으로 움직인 경우 |

**이 검사가 못 잡는 경우가 하나 있다.** 폰이 카메라와 **같은 방향**으로 밀린
경우다. 그건 순수한 스케일 오차인데, 스케일의 출처가 보드뿐이라 대조할 독립
기준이 없다. 오차 크기는 대략 **(폰이 밀린 거리 ÷ baseline)** 이다:

- 30mm 밀림 / 150mm baseline → 실측 16.8% 오차
- 5mm 밀림 / 150mm baseline → 실측 1.1% 오차

→ **폰을 확실히 고정**(벽·책에 기대거나 눕혀서)하고 **baseline 을 크게** 잡는 것이
유일한 대비책이다. 작은 밀림은 baseline 이 크면 묻힌다.

보드까지 거리는 상관없다. 합성 검증에서 35~80cm 전 구간 치수 오차 1.7~2.9% 로
평탄했다. 화면에 온전히 들어오기만 하면 된다.

**왜 이 방식이 나은가** (합성 검증, yaw 3도가 섞인 상황):

| 방식 | 복원 거리 (참값 0.800m) | 치수 (참값 200x150mm) | 오차 |
|---|---|---|---|
| **체커보드 포즈** | **0.794 m** | **195.0 x 145.7** | **2.5% / 2.9%** |
| 평행이동 가정 | 1.110 m | 279.2 x 203.6 | 39.6% / 35.7% |

평행이동 방식은 `R=I` 를 **가정**하므로 yaw 가 조금만 섞여도 무너진다.
체커보드 방식은 그 회전을 **측정**해서 반영한다.

촬영 직후 측정된 baseline·회전·solvePnP 재투영 오차가 출력된다.
여기 회전 값이 커도 괜찮다 — 가정이 아니라 측정값이기 때문이다.

---

## 4단계 (대안) — 본 촬영, 평행이동 방식

체커보드를 장면에 둘 수 없을 때만 쓴다. **`--board` 가 가능하면 그쪽을 쓸 것.**

### 절차

측정할 물체를 놓고:

```bash
./venv/bin/python z_capture.py --mono --baseline-mm 200
```

1. 노트북을 **평평한 책상** 위, 곧은 모서리(자·책등)에 밑면을 붙여 놓는다.
2. SPACE — 왼쪽 위치 촬영.
3. 노트북을 그 모서리를 따라 **오른쪽으로 정확히 200mm 민다.**
   - **들었다 놓지 말 것.** 미끄러뜨려야 회전이 안 섞인다.
   - **화면 각도(힌지)를 건드리지 말 것.** pitch 회전이 그대로 들어간다.
   - 이동량은 자로 잰 값을 `--baseline-mm` 에 그대로 넣는다. **이 값이 미터 단위
     스케일의 유일한 출처다.** 여기 오차가 곧 결과 오차다.
4. SPACE — 오른쪽 위치 촬영. Q 로 종료.

**baseline 정하는 법:** 깊이 오차는 `dZ = Z^2 * 0.5px / (fx * b)` 다.
대상까지 거리의 **1/10 이상**을 권장한다. 촬영 시작할 때 실제 fx 로 계산한
거리별 예상 오차표가 출력된다.

**장면 조건 (중요):**
- 두 장 사이에 장면이 움직이면 안 된다 (사람·커튼·화면 등).
- **깊이가 다른 대상이 여러 개 보이게** 찍는다. 평평한 벽 하나만 찍으면
  Essential 행렬이 퇴화해서 회전 검사가 원리적으로 불가능해진다.
- 물체에 **질감**이 있어야 한다. 무늬 없는 단색 면은 특징점이 안 잡힌다.

촬영 직후 자동으로 검사가 돈다:

| 게이트 | 기준 | 실패하면 |
|---|---|---|
| 평면비 | < 0.90 | 장면이 평면 한 장 — 깊이 차이 있게 다시 |
| 회전 | < 1.0 deg | 회전이 섞임 — 곧은 모서리에 붙여 다시 밀 것 |

**회전이 왜 치명적인가:** 특히 yaw 는 시차에 거의 균일한 offset 을 더한다.
2도 yaw 면 `fx*tan(2도) = 31px` 가 더해져 Z=1.50m 가 1.19m 로 **21% 틀어진다.**
그런데 yaw 는 세로 시차(dy)를 거의 건드리지 않아 눈으로도, dy 로도 안 보인다.
그래서 Essential 행렬로 회전각을 직접 재는 것이다.

---

## 5단계 — 3D 좌표·크기 복원

### YOLO 자동 검출 (권장)

```bash
./venv/bin/python z_reconstruct.py --yolo
```

왼쪽 사진에서 객체를 자동으로 찾아 **각각의 3D 좌표와 크기**를 낸다.
검출 결과는 `detections.png` 로 저장되니 뭘 잡았는지 눈으로 확인할 수 있다.

| 옵션 | 뜻 |
|---|---|
| `--model yolo11n.pt` | 더 빠른 모델 (기본은 정확도 우선 `yolo11m.pt`) |
| `--conf 0.4` | 신뢰도 하한 (기본 0.25) |
| `--classes bottle cup` | COCO 클래스 이름으로 걸러내기 |

모델은 첫 실행 때 자동으로 내려받는다(인터넷 필요).
실시간 루프가 아니라 사진 2장을 한 번 처리하는 것이라 큰 모델을 기본값으로 둔다.

### 수동 지정

```bash
./venv/bin/python z_reconstruct.py
```

왼쪽 사진이 뜬다. 대상을 **드래그**해서 지정하고 ENTER. 여러 개 지정 가능
(전부 같은 3D 맵에 배치된다). 다 했으면 ESC.

- 비대화형: `--roi 400,200,180,140` (여러 번 반복 가능). 이때는 좌표를 그대로 쓴다.
- 실측 대조: `--known-mm 210x297`
- 3D 창 없이 수치만: `--no-view`

**ROI 는 자동으로 조금 넓혀진다.** 검출 박스를 물체에 딱 맞추면 가장자리
특징점이 잘려 크기가 과소평가되기 때문이다(실측: 마진 0px 에서 6.6%/9.7% 과소,
8px 이상에서 4.3%/6.9%). 넓혀도 배경은 깊이 클러스터링이 분리하므로 안전하다.

결과는 `results.json` 에 저장되고 Open3D 3D 맵 창이 뜬다.
파랑 = 왼쪽 촬영 위치, 주황 = 오른쪽 촬영 위치, 바닥 격자 25cm.

---

## 카메라 2대를 쓸 때

두 카메라를 단단히 고정한 뒤:

```bash
./venv/bin/python z_calibrate.py --capture-stereo --index-l 0 --index-r 2
./venv/bin/python z_calibrate.py --run-stereo --ruler-mm 122.0
./venv/bin/python z_capture.py --stereo --index-l 0 --index-r 2
./venv/bin/python z_reconstruct.py
```

`--capture-stereo` 는 **양쪽 다 보드가 검출된 쌍만** 저장한다. 15~20쌍.
`--run-stereo` 가 `calib_stereo.json` 을 만들면 5단계는 완전히 동일하다 —
`z_reconstruct.py` 가 `shots/meta.json` 의 mode 를 보고 알아서 갈린다.

2대 모드는 상대 포즈를 `stereoCalibrate` 가 직접 주므로 회전 검사가 필요 없다.
`--run-stereo` 출력에서 **T의 x성분이 음수**인지 확인할 것. 양수면 2번 카메라가
왼쪽에 있는 것이니 `--index-l/--index-r` 을 바꿔 다시 찍어야 한다.

---

## 조밀 스테레오 + YOLO seg/bbox (`z_object_depth.py`)

두 카메라(웹캠 + 폰 초광각)를 고정하고 `calib_stereo.json` 이 있을 때, 희소 SIFT 대신
Fast-FoundationStereo 로 화소마다 깊이를 구해 객체별 거리·크기를 낸다. 질감 없는 물체도 된다.

```bash
# 반드시 venv_ffs 로 (torch 2.6 고정, 메인 venv 와 다름)
./venv_ffs/bin/python z_object_depth.py --mode seg          # yolo11m-seg 마스크 (기본 권장)
./venv_ffs/bin/python z_object_depth.py --mode bbox         # yolo11m 박스 (비교 기준선)
./venv_ffs/bin/python z_object_depth.py --mode both         # seg 한 번 -> mask/bbox 둘 다 집계
./venv_ffs/bin/python z_object_depth.py --mode seg --scale 0.5 --iters 4   # 빠르게
./venv_ffs/bin/python z_object_depth.py --mode seg --known-m 0.62 --known-idx 1   # 실측 대조
./venv_ffs/bin/python z_object_depth.py --mode seg --lr-check   # 좌우 일관성으로 오매칭 제외 (추론 2회)
```

출력: `out_dense/objects.json`(객체별 Z_rect·dist·p10/p90·크기·상태), `overlay_seg.png` /
`overlay_bbox.png`, `depth_rect_m.npy`, `disp_vis.png`, `valid_mask.png`.

왜 seg 가 기본인가 (합성 검증):
- 박스에 배경이 과반이면 중앙값이 배경 거리가 된다 (마진 90px → 3.97m).
- 크기는 "평면 적합 + 마스크 윤곽" 으로 재므로 깊이 노이즈 2% 에서도 오차 1.2%.
  bbox 는 윤곽이 없어 점군 껍질로 재고, 같은 조건에서 15.8% 로 부푼다.

한계: 정류 품질(캘리 왜곡계수, 특히 폰 초광각 가장자리 커버리지 8/16)과 (R,T) 정확도에 민감.
`에피폴라 잔차` 가 1px 를 넘으면 (R,T) 나 캘리를 먼저 의심할 것. 두께 값은 노이즈 지표일 뿐이다.

## 결과 파일과 상태값 읽는 법

- `results.json`(희소 경로)과 `out_dense/objects.json`(조밀 경로)은 **매 실행마다 새로 쓴다.**
  전부 실패해도 이전 결과가 남지 않는다. 각 객체의 `status` 가 `ok` 인 것만 측정값이다.
  `fail:reproj`, `fail:points`, `ambiguous`, `insufficient_valid_pixels`, `no_mask` 는 진단용이다.
- `distance_m`(카메라 중심까지 유클리드, 줄자 측정에 대응)과 `z_med_m`/`depth_rect_m`(광축 깊이)은 다르다.
- `calib_stereo.json` 의 `scale_status`: `provisional`(비율만 맞음) → `--rescale` 또는 `--baseline-mm` /
  `--known-mm` 으로 `reference_scaled`/`measured` 가 되어야 미터 단위다. 임시 상태면 두 스크립트가 경고를 찍는다.
- `--rescale` 은 results.json 이 **현재** calib_stereo.json 의 baseline 으로 만들어진 것일 때만 받는다
  (중복 보정 방지). 거부되면 `z_reconstruct.py` 를 다시 돌린 뒤 하면 된다.
- 조밀 경로 `ambiguous` 는 영역 안 깊이가 두 덩어리(물체+배경)라 물체를 확정하지 못한 경우다.
  `depth_clusters` 에 두 군집의 크기·깊이·중앙지지가 남으니 보고 판단하고, seg 모드를 쓰면 대개 사라진다.
- 회귀 테스트: `./venv/bin/python z_selftest.py` (GPU·카메라 불필요, 18항목).

## 결과 읽을 때 반드시 알아야 할 한계

1. **앞뒤 두께는 관측되지 않는다.** 두 시점이 가까워 물체의 보이는 앞면만
   복원된다. 박스의 가장 얇은 축은 "물체의 두께"가 아니라 "복원된 표면의
   두께"이며 하한으로만 읽어야 한다. 믿을 수 있는 건 **중심 위치, 폭, 높이**다.

2. **크기는 구조적으로 몇 % 과소평가된다.** SIFT 특징점이 물체 경계까지
   닿지 않기 때문이다. 합성 검증에서 참값 300x200mm 가 287x186mm 로
   나왔다 (4.3% / 6.9%). 더 정확한 치수가 필요하면 희소 매칭 대신
   조밀 스테레오(StereoSGBM)로 가야 한다.

3. **`--board` 는 체커보드가 두 사진에 보여야만 쓸 수 있다.** 안 보이면
   `--mono` 로 물러서야 하고, 그때는 회전 게이트를 반드시 통과해야 한다.

4. **깊이 오차는 거리의 제곱에 비례한다.** `dZ = Z^2 * 0.5px / (fx * b)`.
   먼 물체는 baseline 을 키워야 한다.

5. **배경 분리는 "가장 큰 덩어리"를 고른다.** 물체보다 배경에 특징점이 훨씬
   많으면 배경을 고를 수 있다. 출력의 덩어리 목록을 확인하고, 애매하면
   `--z-range 0.5 2.5` 처럼 거리 범위를 좁힌다.
